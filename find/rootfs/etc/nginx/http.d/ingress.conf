server {
    listen %%port%% default_server;
    allow 172.30.32.2;
    deny all;

    client_max_body_size 0;

    root /usr/share/find;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }
}
